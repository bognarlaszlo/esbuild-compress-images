{
  "name": "esbuild-compress-images",
  "version": "0.0.1",
  "license": "MIT",
  "description": "An esbuild plugin for compressing resolved images using lovell/sharp",
  "keywords": [
    "esbuild",
    "esbuild-plugin",
    "sharp",
    "image",
    "compression"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc -p tsconfig.json"
  },
  "author": "bognarlaszlo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bognarlaszlo/esbuild-compress-images.git"
  },
  "dependencies": {
    "sharp": "^0.29.3"
  },
  "peerDependencies": {
    "esbuild": "^0.14.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.11",
    "@types/sharp": "^0.29.4",
    "typescript": "^4.5.2"
  }
}
